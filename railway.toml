[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# No HTTP healthcheck - gRPC server doesn't serve HTTP
# Railway will use process-level health monitoring instead
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
